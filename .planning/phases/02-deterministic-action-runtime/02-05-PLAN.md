---
phase: 02-deterministic-action-runtime
plan: "05"
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/desktop/src/runtime/actions/executors/windows-media-control-adapter.ts
  - apps/desktop/src/runtime/connectivity/desktop-connectivity-runtime.ts
  - apps/desktop/src/ui/control-panel/DesktopControlPanelModel.ts
  - apps/desktop/src/ui/actions/ActionHistoryPanel.tsx
  - tests/actions/deterministic-action-runtime.spec.ts
  - tests/ui/desktop-control-panel-model.spec.ts
autonomous: true
gap_closure: true
requirements:
  - ACTN-01
  - ACTN-02
  - ACTN-03
  - ACTN-04
  - SAFE-01
must_haves:
  truths:
    - Media control tile actions execute successfully on supported Windows hosts through the default runtime wiring.
    - Desktop control panel consumes runtime history rows with timestamps and final outcomes in production code (not test-only imports).
    - Accepted actions still produce one deterministic terminal feedback event and one matching history row.
  artifacts:
    - path: apps/desktop/src/runtime/actions/executors/windows-media-control-adapter.ts
      provides: Concrete Windows media adapter used by default runtime composition.
    - path: apps/desktop/src/runtime/connectivity/desktop-connectivity-runtime.ts
      provides: Runtime composition that wires the Windows media adapter into the media executor path.
    - path: apps/desktop/src/ui/control-panel/DesktopControlPanelModel.ts
      provides: Desktop control-panel runtime model that includes action history panel data.
    - path: tests/actions/deterministic-action-runtime.spec.ts
      provides: Integration assertions for successful media execution via default runtime path.
    - path: tests/ui/desktop-control-panel-model.spec.ts
      provides: UI wiring test proving action history model is consumed from desktop app runtime code.
  key_links:
    - from: apps/desktop/src/runtime/connectivity/desktop-connectivity-runtime.ts
      to: apps/desktop/src/runtime/actions/executors/windows-media-control-adapter.ts
      via: Runtime composition passes concrete adapter into media executor registry.
      pattern: mediaWindowsAdapter|createActionExecutorRegistry|createWindowsMediaControlAdapter
    - from: apps/desktop/src/ui/control-panel/DesktopControlPanelModel.ts
      to: apps/desktop/src/ui/actions/ActionHistoryPanel.tsx
      via: Control panel model includes runtime-derived recent action history rows.
      pattern: createActionHistoryPanelRuntimeModel|rows|timestamp|outcome
---

<objective>
Close Phase 2 verifier gaps by wiring concrete Windows media execution and production desktop history-panel consumption.

Purpose: satisfy failed ACTN-03 and SAFE-01 truths so deterministic runtime behavior is fully user-observable in default app wiring.
Output: concrete Windows media adapter wiring, desktop control panel runtime model integration, and gap-focused integration/UI tests.
</objective>

<execution_context>
@C:/Users/user/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/user/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-deterministic-action-runtime/02-RESEARCH.md
@.planning/phases/02-deterministic-action-runtime/02-VERIFICATION.md
@.planning/phases/02-deterministic-action-runtime/02-04-SUMMARY.md
@apps/desktop/src/runtime/connectivity/desktop-connectivity-runtime.ts
@apps/desktop/src/runtime/actions/executors/media-control-executor.ts
@apps/desktop/src/ui/actions/ActionHistoryPanel.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement and wire a concrete Windows media adapter in default runtime composition</name>
  <files>apps/desktop/src/runtime/actions/executors/windows-media-control-adapter.ts, apps/desktop/src/runtime/connectivity/desktop-connectivity-runtime.ts</files>
  <action>Create a production `createWindowsMediaControlAdapter` implementation that executes curated commands (`play_pause`, `next`, `previous`, `volume_up`, `volume_down`, `mute_toggle`) through deterministic Windows-safe command invocations and explicit adapter result mapping. Wire this adapter into `DesktopConnectivityRuntime` so `createActionExecutorRegistry` receives a concrete adapter by default when platform is `win32` (while preserving existing override injection for tests). Keep non-win32 behavior unchanged (`unsupported_platform`) and avoid shell-string interpolation.</action>
  <verify>
    <automated>npm run test -- tests/actions/deterministic-action-runtime.spec.ts --runInBand -t "media"</automated>
    <manual>On a Windows host, trigger a media tile and confirm at least one command path reaches terminal `success` instead of `execution_failed` due to `adapter_unavailable`.</manual>
    <sampling_rate>Run after task commit and before Task 2.</sampling_rate>
  </verify>
  <done>Default win32 runtime path includes a concrete media adapter and no longer fails solely because adapter wiring is missing.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate action history panel model into desktop control-panel runtime model</name>
  <files>apps/desktop/src/ui/control-panel/DesktopControlPanelModel.ts, apps/desktop/src/ui/actions/ActionHistoryPanel.tsx</files>
  <action>Add a production desktop control-panel model builder/handler that composes existing panel models and explicitly includes `ActionHistoryPanel` data sourced from `DesktopConnectivityRuntime.getRecentActionHistory(...)`. Ensure the history section includes timestamp and terminal outcome fields and is exposed via a production import path (not only test helpers). Keep scope to model/runtime wiring and do not introduce Phase 3 tile CRUD/editor behavior.</action>
  <verify>
    <automated>npm run test -- tests/ui/desktop-control-panel-model.spec.ts --runInBand</automated>
    <manual>Confirm the new control-panel model returns a `recent actions` section with rows after executing runtime actions.</manual>
    <sampling_rate>Run after task commit and before Task 3.</sampling_rate>
  </verify>
  <done>Desktop app runtime code has a production consumer of `ActionHistoryPanel` with runtime-backed rows, timestamps, and outcomes.</done>
</task>

<task type="auto">
  <name>Task 3: Add gap-focused runtime and UI wiring tests for media success and history consumption</name>
  <files>tests/actions/deterministic-action-runtime.spec.ts, tests/ui/desktop-control-panel-model.spec.ts</files>
  <action>Expand deterministic runtime integration tests to cover successful media execution through the default runtime composition path (not custom injected stubs only), while retaining existing guard and lifecycle assertions. Add a UI wiring test that imports desktop control-panel production model code and verifies it surfaces runtime-owned history rows with newest-first timestamps and final outcomes after executed actions.</action>
  <verify>
    <automated>npm run test -- tests/actions/deterministic-action-runtime.spec.ts tests/ui/desktop-control-panel-model.spec.ts --runInBand</automated>
    <manual>Confirm both verifier gaps are directly covered by assertions tied to production wiring paths.</manual>
    <sampling_rate>Run after task commit and before plan completion.</sampling_rate>
  </verify>
  <done>Automated tests prove ACTN-03 and SAFE-01 are satisfied through production runtime/UI wiring, closing verifier-reported gaps.</done>
</task>

</tasks>

<verification>
- Default `win32` runtime composition executes media controls through a concrete adapter and can emit terminal success for valid commands.
- Desktop control panel runtime model includes recent action history rows from runtime-owned data with timestamps and final outcomes.
- Deterministic lifecycle parity remains intact: each accepted action ends with one terminal feedback event and one matching history row.
</verification>

<success_criteria>
- Verifier gaps for ACTN-03 and SAFE-01 in `.planning/phases/02-deterministic-action-runtime/02-VERIFICATION.md` are resolved by production wiring and automated tests.
- `npm run test -- tests/actions/deterministic-action-runtime.spec.ts tests/ui/desktop-control-panel-model.spec.ts --runInBand` passes.
</success_criteria>

<output>
After completion, create `.planning/phases/02-deterministic-action-runtime/02-05-SUMMARY.md`
</output>
