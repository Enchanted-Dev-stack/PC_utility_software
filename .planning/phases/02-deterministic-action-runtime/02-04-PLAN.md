---
phase: 02-deterministic-action-runtime
plan: "04"
type: execute
wave: 3
depends_on:
  - "02"
  - "03"
files_modified:
  - apps/desktop/src/runtime/actions/action-registry.ts
  - apps/desktop/src/runtime/actions/action-request-runtime.ts
  - apps/desktop/src/runtime/connectivity/desktop-connectivity-runtime.ts
  - apps/mobile/src/runtime/connectivity/mobile-connectivity-client.ts
  - apps/desktop/src/ui/actions/ActionHistoryPanel.tsx
  - tests/actions/deterministic-action-runtime.spec.ts
autonomous: true
requirements:
  - ACTN-01
  - ACTN-02
  - ACTN-03
  - ACTN-04
  - SAFE-01
must_haves:
  truths:
    - Authorized phone actions execute through one deterministic runtime pipeline and return staged feedback.
    - App, website, and media tile actions all produce terminal success/failure outcomes visible to clients.
    - Desktop panel can view recent action history rows with timestamp, action type, and final outcome.
  artifacts:
    - path: apps/desktop/src/runtime/actions/action-request-runtime.ts
      provides: Integration of auth guard + orchestrator + typed registry for accepted action execution.
    - path: apps/desktop/src/runtime/actions/action-registry.ts
      provides: Action type to executor mapping for open-app, open-website, and media-control handlers.
    - path: apps/desktop/src/ui/actions/ActionHistoryPanel.tsx
      provides: Desktop runtime history view model for recent action rows.
    - path: tests/actions/deterministic-action-runtime.spec.ts
      provides: End-to-end deterministic runtime tests across accepted, denied, and failed action paths.
  key_links:
    - from: apps/desktop/src/runtime/actions/action-request-runtime.ts
      to: apps/desktop/src/runtime/actions/action-orchestrator.ts
      via: Guarded request flow hands accepted actions to orchestrator lifecycle pipeline.
      pattern: authorizeAction\(|handleAction\(|received|running|success|failure
    - from: apps/desktop/src/ui/actions/ActionHistoryPanel.tsx
      to: apps/desktop/src/runtime/actions/action-history-store.ts
      via: Panel model derives rows from runtime-owned terminal history entries.
      pattern: getRecent|outcome|completedAt
---

<objective>
Wire deterministic action execution, lifecycle feedback, and desktop history into end-to-end runtime behavior.

Purpose: complete Phase 2 user-visible outcomes by connecting executors, orchestrator lifecycle events, and history UI from one guarded runtime path.
Output: runtime action registry wiring, updated request pipeline, mobile feedback stream hooks, desktop history panel model, and end-to-end deterministic tests.
</objective>

<execution_context>
@C:/Users/user/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/user/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-deterministic-action-runtime/02-RESEARCH.md
@.planning/phases/01-trusted-connectivity-foundation/01-04-SUMMARY.md
@.planning/phases/02-deterministic-action-runtime/02-01-PLAN.md
@.planning/phases/02-deterministic-action-runtime/02-02-PLAN.md
@.planning/phases/02-deterministic-action-runtime/02-03-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire action registry and guarded request runtime into orchestrator lifecycle</name>
  <files>apps/desktop/src/runtime/actions/action-registry.ts, apps/desktop/src/runtime/actions/action-request-runtime.ts, apps/desktop/src/runtime/connectivity/desktop-connectivity-runtime.ts</files>
  <action>Create an action registry mapping `open_app`, `open_website`, and `media_control` to their typed executors. Update `ActionRequestRuntime` to keep existing SessionAuthGuard-first behavior, then route accepted commands to the orchestrator so returned status reflects deterministic lifecycle acceptance (not fire-and-forget dispatch). Integrate this runtime path in desktop connectivity composition so action handling, feedback stream, and history store are all available from one runtime instance.</action>
  <verify>
    <automated>npm run test -- tests/actions/deterministic-action-runtime.spec.ts --runInBand -t "guarded request runtime"</automated>
    <manual>Confirm untrusted/invalid-session requests still return explicit denial reasons before executor invocation.</manual>
    <sampling_rate>Run after task commit and before Task 2.</sampling_rate>
  </verify>
  <done>Guarded request runtime executes only authorized actions through registry-backed orchestrator lifecycle flow.</done>
</task>

<task type="auto">
  <name>Task 2: Add runtime feedback and history models for mobile and desktop surfaces</name>
  <files>apps/mobile/src/runtime/connectivity/mobile-connectivity-client.ts, apps/desktop/src/ui/actions/ActionHistoryPanel.tsx</files>
  <action>Add mobile runtime hooks that can subscribe to action feedback events so phone clients can observe `received`/`running`/terminal states by `actionId`. Add desktop action-history panel model builder that reads recent entries from runtime history store and renders timestamp, action label/type, and outcome status for SAFE-01. Keep this phase focused on runtime/state models and avoid introducing Phase 3 tile editor CRUD concerns.</action>
  <verify>
    <automated>npm run test -- tests/actions/deterministic-action-runtime.spec.ts --runInBand -t "feedback and history models"</automated>
    <manual>Confirm history rows derive from runtime terminal events and not UI-side inferred timers.</manual>
    <sampling_rate>Run after task commit and before Task 3.</sampling_rate>
  </verify>
  <done>Mobile receives deterministic lifecycle feedback stream and desktop history panel model exposes recent action outcomes with timestamps.</done>
</task>

<task type="auto">
  <name>Task 3: Add end-to-end deterministic action runtime verification suite</name>
  <files>tests/actions/deterministic-action-runtime.spec.ts</files>
  <action>Add integration-style tests that run through pairing/trusted session setup, action submission for app/website/media commands, lifecycle feedback assertions (`received` -> `running` -> terminal), and desktop history assertions for final outcomes. Include negative-path tests for invalid URL, unsupported media platform/command failure, and auth-denied requests to prove deterministic error behavior. Verify one terminal event + one history row per accepted action and no history rows for unauthorized requests.</action>
  <verify>
    <automated>npm run test -- tests/actions/deterministic-action-runtime.spec.ts --runInBand</automated>
    <manual>Confirm all Phase 2 success truths are covered by explicit assertions in this suite.</manual>
    <sampling_rate>Run after task commit and before plan completion.</sampling_rate>
  </verify>
  <done>End-to-end runtime tests demonstrate deterministic outcomes and audit visibility for app, website, and media actions.</done>
</task>

</tasks>

<verification>
- Authorized action requests flow through guard -> orchestrator -> typed executor -> terminal feedback -> history append.
- Mobile feedback and desktop history surfaces read from runtime-owned lifecycle/history sources.
- App, website, and media actions all expose deterministic success/failure outcomes.
</verification>

<success_criteria>
- ACTN-01, ACTN-02, ACTN-03, ACTN-04, and SAFE-01 are satisfied by wired runtime behavior and integration tests.
- `npm run test -- tests/actions/deterministic-action-runtime.spec.ts --runInBand` passes.
</success_criteria>

<output>
After completion, create `.planning/phases/02-deterministic-action-runtime/02-04-SUMMARY.md`
</output>
