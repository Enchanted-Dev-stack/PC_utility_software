---
phase: 02-deterministic-action-runtime
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - shared/src/contracts/actions/action-command.ts
  - shared/src/contracts/actions/action-feedback.ts
  - apps/desktop/src/runtime/actions/action-feedback-events.ts
  - apps/desktop/src/runtime/actions/action-history-store.ts
  - apps/desktop/src/runtime/actions/action-orchestrator.ts
  - tests/actions/action-orchestrator.spec.ts
autonomous: true
requirements:
  - ACTN-04
  - SAFE-01
must_haves:
  truths:
    - Every accepted action emits ordered lifecycle feedback states with one terminal outcome.
    - Runtime stores a bounded recent action history with timestamps and final outcome per action.
    - Duplicate action IDs do not execute twice and return deterministic terminal data.
  artifacts:
    - path: shared/src/contracts/actions/action-feedback.ts
      provides: Typed lifecycle states and payload contracts for received, running, success, and failure stages.
    - path: apps/desktop/src/runtime/actions/action-orchestrator.ts
      provides: Guarded queueing + lifecycle emission + dedupe + terminal-history append flow.
    - path: apps/desktop/src/runtime/actions/action-history-store.ts
      provides: Append-only bounded history API with deterministic ordering.
    - path: tests/actions/action-orchestrator.spec.ts
      provides: Deterministic verification for lifecycle ordering, dedupe, and history append behavior.
  key_links:
    - from: apps/desktop/src/runtime/actions/action-orchestrator.ts
      to: shared/src/contracts/actions/action-feedback.ts
      via: Lifecycle emissions use shared feedback contract types.
      pattern: received|running|success|failure
    - from: apps/desktop/src/runtime/actions/action-orchestrator.ts
      to: apps/desktop/src/runtime/actions/action-history-store.ts
      via: Terminal events append one history row with timestamps and outcome.
      pattern: append\(|outcome
---

<objective>
Establish the deterministic runtime backbone for action feedback and audit history.

Purpose: make ACTN-04 and SAFE-01 inherent runtime behavior before action-type executors are added.
Output: shared action contracts, orchestrator lifecycle runtime, bounded history store, and deterministic orchestrator tests.
</objective>

<execution_context>
@C:/Users/user/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/user/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-deterministic-action-runtime/02-RESEARCH.md
@.planning/phases/01-trusted-connectivity-foundation/01-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define shared action command and feedback contracts</name>
  <files>shared/src/contracts/actions/action-command.ts, shared/src/contracts/actions/action-feedback.ts</files>
  <action>Create discriminated union contracts for curated v1 action types (`open_app`, `open_website`, `media_control`) with strongly typed payloads and correlation identifiers (`actionId`, `deviceId`, `hostId`, `sessionId`, requested timestamp). Define lifecycle feedback contracts for ordered `received`, `running`, and terminal `success|failure` states including terminal metadata fields needed for history (`completedAt`, outcome code, optional error taxonomy). Keep contracts backward-compatible with the existing SessionAuthGuard envelope shape and do not introduce Phase 3 editor concerns.</action>
  <verify>
    <automated>npm run test -- tests/actions/action-orchestrator.spec.ts --runInBand -t "contract"</automated>
    <manual>Confirm contract types cover all curated v1 action variants and feedback states without using `any`.</manual>
    <sampling_rate>Run after task commit and before Task 2.</sampling_rate>
  </verify>
  <done>Shared action and feedback contracts exist and compile with explicit typed variants for all Phase 2 runtime action categories.</done>
</task>

<task type="auto">
  <name>Task 2: Implement deterministic orchestrator, feedback bus, and bounded history store</name>
  <files>apps/desktop/src/runtime/actions/action-feedback-events.ts, apps/desktop/src/runtime/actions/action-history-store.ts, apps/desktop/src/runtime/actions/action-orchestrator.ts</files>
  <action>Implement an ActionRuntimeOrchestrator that preserves the existing authorization gate semantics from Phase 1, then emits ordered lifecycle stages (`received` -> `running` -> terminal) for each accepted action. Add deterministic queueing (serial per scoped device key by default) plus actionId dedupe so retries cannot double-execute the same command. Add a runtime-owned append-only history store with bounded retention trimming and terminal-only writes. Ensure failure path always emits terminal `failure` and writes matching history row even when executor throws.</action>
  <verify>
    <automated>npm run test -- tests/actions/action-orchestrator.spec.ts --runInBand -t "orchestrator"</automated>
    <manual>Confirm unauthorized actions never emit `running` and accepted actions always produce exactly one terminal event.</manual>
    <sampling_rate>Run after task commit and before Task 3.</sampling_rate>
  </verify>
  <done>Orchestrator enforces deterministic lifecycle order, dedupe, and bounded terminal history writes from one central runtime path.</done>
</task>

<task type="auto">
  <name>Task 3: Add deterministic orchestrator tests for lifecycle, dedupe, and history integrity</name>
  <files>tests/actions/action-orchestrator.spec.ts</files>
  <action>Create integration-style Jest coverage for authorized success path, executor failure path, unauthorized short-circuit path, duplicate `actionId` handling, and history retention trimming. Assert strict stage ordering per action, exactly one terminal stage per action, and outcome parity between emitted terminal event and stored history row. Keep test runtime under 60 seconds using in-memory fakes for executor and history dependencies.</action>
  <verify>
    <automated>npm run test -- tests/actions/action-orchestrator.spec.ts --runInBand</automated>
    <manual>Review failing/passing snapshots to verify no race-dependent ordering assumptions remain.</manual>
    <sampling_rate>Run after task commit and before plan completion.</sampling_rate>
  </verify>
  <done>Automated tests prove deterministic lifecycle and history behavior for all orchestrator core paths.</done>
</task>

</tasks>

<verification>
- Accepted actions always emit ordered lifecycle feedback from `received` to terminal state.
- Runtime deduplicates duplicate action IDs and avoids duplicate execution.
- Action history is runtime-owned, terminal-only, bounded, and timestamped.
</verification>

<success_criteria>
- ACTN-04 lifecycle model and SAFE-01 history backbone are implemented in shared/runtime layers.
- `npm run test -- tests/actions/action-orchestrator.spec.ts --runInBand` passes.
</success_criteria>

<output>
After completion, create `.planning/phases/02-deterministic-action-runtime/02-01-SUMMARY.md`
</output>
