---
phase: 02-deterministic-action-runtime
plan: "03"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - apps/desktop/src/runtime/actions/executors/media-control-executor.ts
  - tests/actions/media-control-executor.spec.ts
autonomous: true
requirements:
  - ACTN-03
must_haves:
  truths:
    - Media control actions map to explicit supported commands and return deterministic outcomes.
    - Unsupported platforms return explicit failure instead of silent success.
    - Media command execution failures surface stable error codes for ACTN-04 feedback.
  artifacts:
    - path: apps/desktop/src/runtime/actions/executors/media-control-executor.ts
      provides: Typed media command executor with command enum mapping and platform-aware adapter responses.
    - path: tests/actions/media-control-executor.spec.ts
      provides: Automated verification for supported command handling, unsupported platform handling, and failure code mapping.
  key_links:
    - from: apps/desktop/src/runtime/actions/executors/media-control-executor.ts
      to: shared/src/contracts/actions/action-command.ts
      via: Command payload enum validates play/pause/next/previous/volume actions.
      pattern: media_control|play_pause|next|previous|volume
---

<objective>
Implement deterministic media control execution for the curated runtime action set.

Purpose: satisfy ACTN-03 with explicit command mapping and platform-aware error handling that aligns with lifecycle feedback.
Output: media-control executor and focused tests for supported/unsupported/failure outcomes.
</objective>

<execution_context>
@C:/Users/user/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/user/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-deterministic-action-runtime/02-RESEARCH.md
@.planning/phases/02-deterministic-action-runtime/02-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build typed media-control executor with explicit command mapping</name>
  <files>apps/desktop/src/runtime/actions/executors/media-control-executor.ts</files>
  <action>Implement a `media_control` executor that accepts only curated commands (`play_pause`, `next`, `previous`, `volume_up`, `volume_down`, `mute_toggle`) and maps them to a platform adapter abstraction. Make Windows the primary implementation target for v1 and return `unsupported_platform` for other platforms unless adapter support is explicitly provided. Ensure executor returns deterministic result codes (`success`, `command_failed`, `unsupported_platform`, `invalid_payload`) without throwing unclassified errors upstream.</action>
  <verify>
    <automated>npm run test -- tests/actions/media-control-executor.spec.ts --runInBand -t "media control"</automated>
    <manual>Confirm unknown command values return `invalid_payload` and do not call the adapter.</manual>
    <sampling_rate>Run after task commit and before Task 2.</sampling_rate>
  </verify>
  <done>Media-control executor exists with explicit command enum handling and deterministic platform/error outcomes.</done>
</task>

<task type="auto">
  <name>Task 2: Add media executor tests for command coverage and failure semantics</name>
  <files>tests/actions/media-control-executor.spec.ts</files>
  <action>Add Jest coverage for each curated command path, adapter-level command failure mapping, invalid command payload rejection, and unsupported platform behavior. Keep tests deterministic by injecting mock adapter responses and asserting exact outcome codes returned to the orchestrator layer.</action>
  <verify>
    <automated>npm run test -- tests/actions/media-control-executor.spec.ts --runInBand</automated>
    <manual>Confirm tests verify response codes rather than only checking that methods were called.</manual>
    <sampling_rate>Run after task commit and before plan completion.</sampling_rate>
  </verify>
  <done>Automated coverage proves ACTN-03 command handling and failure semantics are deterministic and explicit.</done>
</task>

</tasks>

<verification>
- Runtime accepts only curated media control commands.
- Unsupported platform behavior is explicit and deterministic.
- Adapter failures map to stable result codes for lifecycle feedback consumers.
</verification>

<success_criteria>
- ACTN-03 media control executor behavior is implemented with deterministic outcomes.
- `npm run test -- tests/actions/media-control-executor.spec.ts --runInBand` passes.
</success_criteria>

<output>
After completion, create `.planning/phases/02-deterministic-action-runtime/02-03-SUMMARY.md`
</output>
