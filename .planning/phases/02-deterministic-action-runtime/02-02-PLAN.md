---
phase: 02-deterministic-action-runtime
plan: "02"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - apps/desktop/src/runtime/actions/executors/open-app-executor.ts
  - apps/desktop/src/runtime/actions/executors/open-url-executor.ts
  - tests/actions/open-target-executors.spec.ts
autonomous: true
requirements:
  - ACTN-01
  - ACTN-02
must_haves:
  truths:
    - App-launch actions resolve only to curated allowed desktop targets and report deterministic success/failure.
    - Website actions accept only valid HTTP/HTTPS URLs and report deterministic success/failure.
    - Launch executor failures return stable runtime error taxonomy values.
  artifacts:
    - path: apps/desktop/src/runtime/actions/executors/open-app-executor.ts
      provides: Allowlisted app launcher executor with safe process invocation and typed outcomes.
    - path: apps/desktop/src/runtime/actions/executors/open-url-executor.ts
      provides: URL validator + launcher executor for HTTP/HTTPS targets.
    - path: tests/actions/open-target-executors.spec.ts
      provides: Unit/integration verification for allowlist checks, URL validation, and launch outcomes.
  key_links:
    - from: apps/desktop/src/runtime/actions/executors/open-url-executor.ts
      to: node:url
      via: WHATWG URL parsing and protocol gating before launch.
      pattern: URL\.canParse|new URL
    - from: apps/desktop/src/runtime/actions/executors/open-app-executor.ts
      to: node:child_process
      via: Safe argument-array process start (no shell concatenation).
      pattern: spawn\(|execFile\(
---

<objective>
Implement deterministic app and website execution adapters for curated action tiles.

Purpose: satisfy ACTN-01 and ACTN-02 with safe, typed, and testable desktop executors before final runtime wiring.
Output: open-app/open-url executors with input validation, launcher behavior, and focused automated tests.
</objective>

<execution_context>
@C:/Users/user/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/user/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-deterministic-action-runtime/02-RESEARCH.md
@.planning/phases/02-deterministic-action-runtime/02-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build allowlisted open-application executor</name>
  <files>apps/desktop/src/runtime/actions/executors/open-app-executor.ts</files>
  <action>Implement a typed `open_app` executor that resolves incoming `appKey` values against a curated allowlist map (no arbitrary path execution). Execute targets using `spawn`/`execFile` with argument arrays and shell disabled by default. Return normalized result codes for `success`, `app_not_found`, `launch_failed`, and `unsupported_platform` (if platform-specific mapping is unavailable). Preserve deterministic metadata needed by orchestrator terminal events and history rows.</action>
  <verify>
    <automated>npm run test -- tests/actions/open-target-executors.spec.ts --runInBand -t "open application"</automated>
    <manual>Confirm unknown app keys are rejected before process execution is attempted.</manual>
    <sampling_rate>Run after task commit and before Task 2.</sampling_rate>
  </verify>
  <done>Open-app executor safely launches only curated targets and emits stable outcome codes.</done>
</task>

<task type="auto">
  <name>Task 2: Build validated open-website executor</name>
  <files>apps/desktop/src/runtime/actions/executors/open-url-executor.ts</files>
  <action>Implement a typed `open_website` executor that validates and normalizes input with WHATWG URL (`URL.canParse` + `new URL`) and accepts only `http:`/`https:` protocols. Reject malformed/unsafe protocols with explicit `invalid_url` outcome. Launch validated URLs via a platform launcher adapter and return deterministic `success` or `launch_failed`/`unsupported_platform` outcomes that match orchestrator error taxonomy conventions.</action>
  <verify>
    <automated>npm run test -- tests/actions/open-target-executors.spec.ts --runInBand -t "open website"</automated>
    <manual>Confirm non-http protocols (for example `file:` or `javascript:`) are rejected with `invalid_url`.</manual>
    <sampling_rate>Run after task commit and before Task 3.</sampling_rate>
  </verify>
  <done>Open-website executor performs strict URL validation and returns deterministic launch outcomes.</done>
</task>

<task type="auto">
  <name>Task 3: Add executor tests for app and website runtime outcomes</name>
  <files>tests/actions/open-target-executors.spec.ts</files>
  <action>Add Jest coverage for happy paths and failure paths across both executors: allowlisted app success, missing app key rejection, launcher failure mapping, valid URL success, invalid URL rejection, and unsupported protocol rejection. Ensure assertions verify returned typed outcome codes and that command launch APIs are called with non-shell argument arrays.</action>
  <verify>
    <automated>npm run test -- tests/actions/open-target-executors.spec.ts --runInBand</automated>
    <manual>Review test names and expectations to confirm ACTN-01/ACTN-02 behavior is observable from runtime outputs.</manual>
    <sampling_rate>Run after task commit and before plan completion.</sampling_rate>
  </verify>
  <done>Automated tests prove deterministic success/failure semantics for app and website action executors.</done>
</task>

</tasks>

<verification>
- App launcher accepts only curated keys and does not execute arbitrary payload strings.
- Website launcher validates URL shape/protocol before any launch attempt.
- Executor outcomes map to deterministic runtime status codes used by lifecycle feedback.
</verification>

<success_criteria>
- ACTN-01 and ACTN-02 executor behavior is implemented and test-covered.
- `npm run test -- tests/actions/open-target-executors.spec.ts --runInBand` passes.
</success_criteria>

<output>
After completion, create `.planning/phases/02-deterministic-action-runtime/02-02-SUMMARY.md`
</output>
