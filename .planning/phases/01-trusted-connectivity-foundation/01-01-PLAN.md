---
phase: 01-trusted-connectivity-foundation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/desktop/src/connectivity/discovery/discovery-service.ts
  - apps/desktop/src/connectivity/discovery/manual-connect.ts
  - apps/mobile/src/connectivity/discovery/useHostDiscovery.ts
  - shared/src/contracts/connectivity/discovery.ts
  - tests/connectivity/discovery.spec.ts
autonomous: true
requirements:
  - CNCT-01
must_haves:
  truths:
    - User can discover a PC host on the same Wi-Fi network.
    - User can manually enter an IP and connect when discovery does not find hosts.
    - User can retry scan and see an updated host list with host identity metadata.
  artifacts:
    - path: apps/desktop/src/connectivity/discovery/discovery-service.ts
      provides: LAN host advertisement and discovery response metadata.
    - path: apps/mobile/src/connectivity/discovery/useHostDiscovery.ts
      provides: Auto-scan, retry, and host list state for phone UI.
    - path: apps/desktop/src/connectivity/discovery/manual-connect.ts
      provides: Manual IP fallback handshake endpoint and host persistence hook.
  key_links:
    - from: apps/mobile/src/connectivity/discovery/useHostDiscovery.ts
      to: shared/src/contracts/connectivity/discovery.ts
      via: Typed discovery envelope for scan and host candidate payloads.
      pattern: scan_request|scan_result|manual_connect
    - from: apps/mobile/src/connectivity/discovery/useHostDiscovery.ts
      to: apps/desktop/src/connectivity/discovery/manual-connect.ts
      via: Manual connect request to direct host endpoint.
      pattern: submitManualIp|manual_connect
---

<objective>
Build the Phase 1 discovery foundation so a phone can always find or reach the PC host on LAN.

Purpose: satisfy CNCT-01 with deterministic auto-scan and manual IP fallback.
Output: shared discovery contract, desktop discovery handlers, mobile discovery workflow, and focused discovery tests.
</objective>

<execution_context>
@C:/Users/user/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/user/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-trusted-connectivity-foundation/01-CONTEXT.md
@.planning/phases/01-trusted-connectivity-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared discovery contract and desktop discovery service</name>
  <files>shared/src/contracts/connectivity/discovery.ts, apps/desktop/src/connectivity/discovery/discovery-service.ts</files>
  <action>Define typed discovery messages containing hostId, hostName, deviceId, and lastSeen. Implement desktop discovery advertisement and response handling using LAN discovery adapter so phone auto-scan can enumerate one or many hosts with stable metadata.</action>
  <verify>
    <automated>npm run test -- tests/connectivity/discovery.spec.ts --runInBand</automated>
    <manual>Verify discovery payload includes hostId, hostName, deviceId, and lastSeen.</manual>
    <sampling_rate>Run after task commit and before Task 2.</sampling_rate>
  </verify>
  <done>Desktop emits deterministic discovery responses and shared contract compiles with no schema mismatch.</done>
</task>

<task type="auto">
  <name>Task 2: Implement manual IP fallback and last-successful host persistence hook</name>
  <files>apps/desktop/src/connectivity/discovery/manual-connect.ts, apps/mobile/src/connectivity/discovery/useHostDiscovery.ts</files>
  <action>Add manual connect request/response flow that validates IP input, attempts handshake, and returns host metadata on success. Update mobile discovery hook to auto-scan on open, expose Retry scan and Enter IP manually actions, and persist last successful host reference for next reconnect attempt.</action>
  <verify>
    <automated>npm run test -- tests/connectivity/discovery.spec.ts --runInBand</automated>
    <manual>Simulate no-host scenario and confirm Retry scan and Enter IP manually are available.</manual>
    <sampling_rate>Run after task commit and before plan completion.</sampling_rate>
  </verify>
  <done>Phone can connect with manual IP when scan misses hosts, and last successful host is persisted for future reconnect flow.</done>
</task>

</tasks>

<verification>
- Discovery scan returns host candidates with required metadata.
- Manual IP fallback succeeds with valid host and fails with explicit error for invalid/unreachable host.
- Auto-scan starts immediately when phone opens the connection screen.
</verification>

<success_criteria>
- CNCT-01 is fully covered by executable discovery and fallback flows.
- All discovery payloads are validated by shared contract types.
- Discovery tests pass via documented command.
</success_criteria>

<output>
After completion, create `.planning/phases/01-trusted-connectivity-foundation/01-01-SUMMARY.md`
</output>
