---
phase: 01-trusted-connectivity-foundation
plan: "04"
type: execute
wave: 4
depends_on:
  - "03"
files_modified:
  - apps/desktop/src/runtime/connectivity/desktop-connectivity-runtime.ts
  - apps/desktop/src/runtime/connectivity/in-memory-lan-discovery-adapter.ts
  - apps/desktop/src/runtime/actions/action-request-runtime.ts
  - apps/mobile/src/runtime/connectivity/mobile-connectivity-client.ts
  - apps/mobile/src/runtime/connectivity/connection-screen-runtime.ts
  - apps/desktop/src/ui/connection-status/DesktopConnectionStatusBanner.tsx
  - apps/desktop/src/ui/trusted-devices/TrustedDevicesPanel.tsx
  - tests/connectivity/runtime-wiring.spec.ts
autonomous: true
gap_closure: true
requirements:
  - CNCT-01
  - CNCT-02
  - CNCT-03
  - CNCT-04
  - SAFE-02
  - SAFE-03
must_haves:
  truths:
    - User can discover the PC on LAN or connect with manual IP using real runtime wiring.
    - User can complete pairing and reconnect on real transport, and untrusted devices are denied actions.
    - User can see connected/reconnecting/disconnected state on desktop with active host + trusted indicator in the main header and revoke a trusted device from live UI handlers.
    - Runtime state changes trigger subtle status transitions and concise toasts across status surfaces.
  artifacts:
    - path: apps/desktop/src/runtime/connectivity/desktop-connectivity-runtime.ts
      provides: Production runtime composition for discovery, pairing, reconnect, and trust lifecycle services.
    - path: apps/mobile/src/runtime/connectivity/mobile-connectivity-client.ts
      provides: Concrete mobile discovery/pairing/reconnect client wired to desktop runtime endpoints.
    - path: apps/desktop/src/runtime/actions/action-request-runtime.ts
      provides: Action request pipeline that enforces SessionAuthGuard before dispatch.
    - path: apps/desktop/src/ui/connection-status/DesktopConnectionStatusBanner.tsx
      provides: Desktop connected/reconnecting/disconnected status model for control panel rendering.
    - path: tests/connectivity/runtime-wiring.spec.ts
      provides: End-to-end runtime wiring verification for discovery, pairing, reconnect, status, and revoke flows.
  key_links:
    - from: apps/mobile/src/runtime/connectivity/mobile-connectivity-client.ts
      to: apps/desktop/src/runtime/connectivity/desktop-connectivity-runtime.ts
      via: Concrete scan/manual-connect/pairing/reconnect method calls over runtime bridge.
      pattern: scanHosts|manualConnect|requestPairing|getPairingStatus|connectToHost
    - from: apps/desktop/src/runtime/actions/action-request-runtime.ts
      to: apps/desktop/src/connectivity/session/session-auth-guard.ts
      via: Guard authorization before action dispatch.
      pattern: authorizeAction\(|untrusted_device|invalid_session
    - from: apps/desktop/src/ui/trusted-devices/TrustedDevicesPanel.tsx
      to: apps/desktop/src/runtime/connectivity/desktop-connectivity-runtime.ts
      via: Live revoke event handlers and status refresh feed.
      pattern: revokeDeviceFromPanel|buildTrustedDevicesPanelModel|connection state
---

<objective>
Close Phase 1 verification blockers by replacing interface-only connectivity paths with executable runtime wiring.

Purpose: make previously model-only discovery, pairing, reconnect, status, and revocation flows user-observable end-to-end.
Output: concrete desktop/mobile runtime adapters, action-auth pipeline wiring, desktop status surface, and runtime integration tests.
</objective>

<execution_context>
@C:/Users/user/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/user/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-trusted-connectivity-foundation/01-VERIFICATION.md
@.planning/phases/01-trusted-connectivity-foundation/01-01-SUMMARY.md
@.planning/phases/01-trusted-connectivity-foundation/01-02-SUMMARY.md
@.planning/phases/01-trusted-connectivity-foundation/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement concrete desktop and mobile runtime connectivity adapters</name>
  <files>apps/desktop/src/runtime/connectivity/in-memory-lan-discovery-adapter.ts, apps/desktop/src/runtime/connectivity/desktop-connectivity-runtime.ts, apps/mobile/src/runtime/connectivity/mobile-connectivity-client.ts, apps/mobile/src/runtime/connectivity/connection-screen-runtime.ts</files>
  <action>Create production (non-test-double) runtime adapters for LAN discovery broadcast/scan response and manual-connect handshake. Compose these adapters with existing DiscoveryService, ManualConnectService, PairingService, TrustedDeviceStore, and ReconnectFlow into a runtime module with explicit methods for scanHosts, manualConnect, requestPairing, getPairingStatus, and connectToHost. Wire connection-screen lifecycle to call openConnectionScreen immediately and persist last successful host for reconnect defaults so verifier gap #1 and transport portions of gap #2 are closed directly.</action>
  <verify>
    <automated>npm run test -- tests/connectivity/runtime-wiring.spec.ts --runInBand -t "runtime connectivity adapters"</automated>
    <manual>Instantiate runtime and confirm mobile client receives host list, manual-connect success/failure, and pairing pending/approved transitions without test-only mocks.</manual>
    <sampling_rate>Run after task commit and before Task 2.</sampling_rate>
  </verify>
  <done>Discovery/manual-connect/pairing/reconnect clients are concrete runtime implementations used outside tests, and connection screen lifecycle is bound to real runtime calls.</done>
</task>

<task type="auto">
  <name>Task 2: Wire action authorization and desktop status plus revoke handlers into live runtime</name>
  <files>apps/desktop/src/runtime/actions/action-request-runtime.ts, apps/desktop/src/ui/connection-status/DesktopConnectionStatusBanner.tsx, apps/desktop/src/ui/trusted-devices/TrustedDevicesPanel.tsx, apps/desktop/src/runtime/connectivity/desktop-connectivity-runtime.ts</files>
  <action>Add a concrete action-request runtime pipeline that routes every inbound action envelope through SessionAuthGuard and returns explicit untrusted_device or invalid_session errors before dispatch. Add a desktop status banner model/component that mirrors connected/reconnecting/disconnected states and reason hints from reconnect/session state snapshots, includes active host + trusted indicator in the main header model, and emits concise toast events plus subtle transition state metadata for status changes. Update trusted-device panel wiring so revoke actions execute through runtime event handlers and refresh status/trusted lists in the same runtime context, directly addressing verifier gap #3 and SAFE-02/SAFE-03 runtime-orphan findings.</action>
  <verify>
    <automated>npm run test -- tests/connectivity/runtime-wiring.spec.ts --runInBand -t "action guard and desktop status wiring"</automated>
    <manual>Run revoke flow from desktop runtime handler and confirm revoked device loses authorization immediately while desktop status remains synchronized, main header shows active host + trusted indicator correctly, and concise status toasts/transitions appear on reconnect/disconnect/revoke changes.</manual>
    <sampling_rate>Run after task commit and before Task 3.</sampling_rate>
  </verify>
  <done>Desktop exposes live connection status with main-header active-host/trusted indicator plus status toasts/transitions, revoke controls call real runtime handlers, and action requests are guard-enforced in production pipeline.</done>
</task>

<task type="auto">
  <name>Task 3: Add gap-focused runtime integration coverage for verifier truths</name>
  <files>tests/connectivity/runtime-wiring.spec.ts</files>
  <action>Add integration tests that execute runtime modules end-to-end (no test-only adapter substitution) for: LAN scan plus manual IP fallback, pairing pending/approve and reconnect to last host, guard denial for revoked/untrusted devices, and desktop status model transitions. Cover each failed truth from 01-VERIFICATION.md with at least one deterministic assertion and keep command runtime under 60 seconds.</action>
  <verify>
    <automated>npm run test -- tests/connectivity/runtime-wiring.spec.ts --runInBand</automated>
    <manual>Confirm each previously failed verification truth has a passing test block and no longer reports as interface-only wiring.</manual>
    <sampling_rate>Run after task commit and before plan completion.</sampling_rate>
  </verify>
  <done>Automated runtime-wiring tests prove all three verification gaps are closed and Phase 1 behaviors are executable in repository runtime code, not only domain abstractions.</done>
</task>

</tasks>

<verification>
- Runtime connectivity path executes discovery, manual fallback, pairing, and reconnect without interface-only placeholders.
- SessionAuthGuard is invoked by concrete action pipeline and blocks revoked/untrusted requests with explicit reasons.
- Desktop and mobile status surfaces consume the same connected/reconnecting/disconnected runtime state transitions, including subtle transition cues and concise toasts.
- Main header status model shows active host and trusted indicator sourced from runtime trust/session state.
- Revocation from desktop panel invalidates trust and causes immediate authorization denial for subsequent action attempts.
</verification>

<success_criteria>
- All verifier gaps in `01-VERIFICATION.md` are directly addressed by runtime wiring artifacts and tests.
- CNCT-01, CNCT-02, CNCT-03, CNCT-04, SAFE-02, and SAFE-03 are covered by executable (not interface-only) code paths.
- `npm run test -- tests/connectivity/runtime-wiring.spec.ts --runInBand` passes.
</success_criteria>

<output>
After completion, create `.planning/phases/01-trusted-connectivity-foundation/01-04-SUMMARY.md`
</output>
