---
phase: 01-trusted-connectivity-foundation
plan: "03"
type: execute
wave: 3
depends_on:
  - "01"
  - "02"
files_modified:
  - apps/desktop/src/connectivity/session/reconnect-state-machine.ts
  - apps/desktop/src/connectivity/trust/revoke-trusted-device.ts
  - apps/desktop/src/ui/trusted-devices/TrustedDevicesPanel.tsx
  - apps/mobile/src/connectivity/session/useReconnectFlow.ts
  - apps/mobile/src/ui/connection-status/ConnectionStatusBanner.tsx
  - apps/mobile/src/ui/controls/ActionTilesGate.tsx
  - tests/connectivity/reconnect-status-revoke.spec.ts
autonomous: true
requirements:
  - CNCT-03
  - CNCT-04
  - SAFE-02
must_haves:
  truths:
    - Trusted devices reconnect to last host without repeating initial pairing.
    - Phone and PC both show live status states: connected, reconnecting, disconnected.
    - User can revoke a trusted phone from PC, and that device immediately loses authorization.
  artifacts:
    - path: apps/desktop/src/connectivity/session/reconnect-state-machine.ts
      provides: Retry/backoff reconnect lifecycle with bounded timeout.
    - path: apps/mobile/src/ui/connection-status/ConnectionStatusBanner.tsx
      provides: User-visible status labels, reason hints, and reconnect CTA.
    - path: apps/desktop/src/ui/trusted-devices/TrustedDevicesPanel.tsx
      provides: Trusted device list and revoke action entry point.
  key_links:
    - from: apps/mobile/src/connectivity/session/useReconnectFlow.ts
      to: apps/desktop/src/connectivity/session/reconnect-state-machine.ts
      via: Shared status events and retry outcome messages.
      pattern: connected|reconnecting|disconnected
    - from: apps/desktop/src/ui/trusted-devices/TrustedDevicesPanel.tsx
      to: apps/desktop/src/connectivity/trust/revoke-trusted-device.ts
      via: Revoke action triggers trust removal and active session termination.
      pattern: revokeTrustedDevice|invalidateSession
---

<objective>
Deliver reconnect UX, live status visibility, and trusted-device revocation controls.

Purpose: satisfy CNCT-03, CNCT-04, and SAFE-02 by closing the trust lifecycle and session resilience loop.
Output: reconnect state machine, status UI components, trusted-device revocation flow, and end-to-end phase behavior tests.
</objective>

<execution_context>
@C:/Users/user/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/user/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-trusted-connectivity-foundation/01-CONTEXT.md
@.planning/phases/01-trusted-connectivity-foundation/01-RESEARCH.md
@.planning/phases/01-trusted-connectivity-foundation/01-01-SUMMARY.md
@.planning/phases/01-trusted-connectivity-foundation/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement reconnect state machine and action-gating behavior</name>
  <files>apps/desktop/src/connectivity/session/reconnect-state-machine.ts, apps/mobile/src/connectivity/session/useReconnectFlow.ts, apps/mobile/src/ui/controls/ActionTilesGate.tsx</files>
  <action>Implement reconnect flow for previously trusted devices that targets last successful host by default, exposes switch-host during retry, applies bounded retry backoff (30-60s window), and disables action tiles while reconnecting/disconnected to prevent stale triggers.</action>
  <verify>
    <automated>npm run test -- tests/connectivity/reconnect-status-revoke.spec.ts --runInBand</automated>
    <manual>Drop connection and confirm status transitions plus disabled action tiles until reconnection succeeds.</manual>
    <sampling_rate>Run after task commit and before Task 2.</sampling_rate>
  </verify>
  <done>Trusted device reconnects automatically, retries are bounded, and action tiles are blocked when not fully connected.</done>
</task>

<task type="auto">
  <name>Task 2: Add dual-surface status UX and trusted-device revocation flow</name>
  <files>apps/mobile/src/ui/connection-status/ConnectionStatusBanner.tsx, apps/desktop/src/ui/trusted-devices/TrustedDevicesPanel.tsx, apps/desktop/src/connectivity/trust/revoke-trusted-device.ts</files>
  <action>Render consistent Connected/Reconnecting/Disconnected labels with reason hints and CTA on mobile and desktop surfaces. Implement trusted-device panel actions to revoke a device, remove trust record, terminate active sessions for that device, and force unauthorized state on subsequent requests.</action>
  <verify>
    <automated>npm run test -- tests/connectivity/reconnect-status-revoke.spec.ts --runInBand</automated>
    <manual>Revoke a previously trusted device from desktop panel and confirm the phone must re-pair before control actions are accepted.</manual>
    <sampling_rate>Run after task commit and before plan completion.</sampling_rate>
  </verify>
  <done>Status is visible in both interfaces and revocation immediately removes authorization for the removed device.</done>
</task>

</tasks>

<verification>
- Reconnect behavior uses backoff with explicit stop window and clear CTA.
- Both mobile and desktop render aligned live connection states.
- Revoked device cannot send actions until re-paired.
</verification>

<success_criteria>
- CNCT-03, CNCT-04, and SAFE-02 are fully covered by implemented flows.
- Live status and reconnect behavior are testable and deterministic.
- Revocation test scenario passes and matches trust lifecycle expectations.
</success_criteria>

<output>
After completion, create `.planning/phases/01-trusted-connectivity-foundation/01-03-SUMMARY.md`
</output>
