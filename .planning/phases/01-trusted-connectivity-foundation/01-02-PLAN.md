---
phase: 01-trusted-connectivity-foundation
plan: "02"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - apps/desktop/src/connectivity/pairing/pairing-service.ts
  - apps/desktop/src/connectivity/trust/trust-store.ts
  - apps/desktop/src/connectivity/session/session-auth-guard.ts
  - apps/mobile/src/connectivity/pairing/usePairingFlow.ts
  - apps/mobile/src/connectivity/pairing/PairingScreen.tsx
  - tests/connectivity/pairing-auth.spec.ts
autonomous: true
requirements:
  - CNCT-02
  - SAFE-03
must_haves:
  truths:
    - User can pair with QR or 6-digit code and pairing requires explicit PC approval.
    - Unpaired or unauthorized devices are denied action requests.
    - Approved devices remain trusted across sessions until revoked.
  artifacts:
    - path: apps/desktop/src/connectivity/pairing/pairing-service.ts
      provides: Pairing challenge generation, approval/denial flow, and trust enrollment.
    - path: apps/desktop/src/connectivity/session/session-auth-guard.ts
      provides: Mandatory trust and session validation for action requests.
    - path: apps/mobile/src/connectivity/pairing/usePairingFlow.ts
      provides: Phone-side QR/code pairing initiation and pending/approved state handling.
  key_links:
    - from: apps/mobile/src/connectivity/pairing/usePairingFlow.ts
      to: apps/desktop/src/connectivity/pairing/pairing-service.ts
      via: Pairing request + approval status events.
      pattern: pairing_request|pairing_pending|pairing_approved|pairing_denied
    - from: apps/desktop/src/connectivity/session/session-auth-guard.ts
      to: apps/desktop/src/connectivity/trust/trust-store.ts
      via: Trust lookup before any action dispatch.
      pattern: isTrusted|validateSession
---

<objective>
Implement explicit trust pairing and hard authorization gating for all control actions.

Purpose: satisfy CNCT-02 and SAFE-03 by making trust explicit, persistent, and server-authoritative.
Output: pairing services, trust store, session auth guard, mobile pairing UX, and auth coverage tests.
</objective>

<execution_context>
@C:/Users/user/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/user/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-trusted-connectivity-foundation/01-CONTEXT.md
@.planning/phases/01-trusted-connectivity-foundation/01-RESEARCH.md
@.planning/phases/01-trusted-connectivity-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build pairing challenge flow with explicit PC approval gate</name>
  <files>apps/desktop/src/connectivity/pairing/pairing-service.ts, apps/mobile/src/connectivity/pairing/usePairingFlow.ts, apps/mobile/src/connectivity/pairing/PairingScreen.tsx</files>
  <action>Implement QR and 6-digit code pairing modes initiated from either phone or PC. Ensure every new pairing enters pending status and only becomes trusted after an explicit desktop Approve action; Deny path must return a clear failure reason to phone UI.</action>
  <verify>
    <automated>npm run test -- tests/connectivity/pairing-auth.spec.ts --runInBand</automated>
    <manual>Validate both QR and code flows reach pending then approved/denied states.</manual>
    <sampling_rate>Run after task commit and before Task 2.</sampling_rate>
  </verify>
  <done>Pairing cannot complete without explicit PC approval and both pairing modes are functional.</done>
</task>

<task type="auto">
  <name>Task 2: Persist trust records and block actions for untrusted sessions</name>
  <files>apps/desktop/src/connectivity/trust/trust-store.ts, apps/desktop/src/connectivity/session/session-auth-guard.ts</files>
  <action>Create persistent trusted-device store keyed by deviceId and host scope. Add session auth guard that rejects action requests unless both trust record and valid active session are present; emit explicit unauthorized reason for denied requests.</action>
  <verify>
    <automated>npm run test -- tests/connectivity/pairing-auth.spec.ts --runInBand</automated>
    <manual>Attempt action from unpaired device and confirm denial response is surfaced.</manual>
    <sampling_rate>Run after task commit and before plan completion.</sampling_rate>
  </verify>
  <done>Unpaired devices cannot execute actions and approved pairings persist across reconnects.</done>
</task>

</tasks>

<verification>
- Pairing requires explicit desktop approval for all new devices.
- Both QR and 6-digit code pairing paths are tested.
- Action channel rejects untrusted or expired sessions deterministically.
</verification>

<success_criteria>
- CNCT-02 and SAFE-03 are fully implemented and verifiable.
- Trust records persist and are consumed by action authorization middleware.
- Auth and pairing tests pass via documented command.
</success_criteria>

<output>
After completion, create `.planning/phases/01-trusted-connectivity-foundation/01-02-SUMMARY.md`
</output>
