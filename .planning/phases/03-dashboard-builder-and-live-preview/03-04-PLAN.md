---
phase: 03-dashboard-builder-and-live-preview
plan: "04"
type: execute
wave: 3
depends_on:
  - "02"
  - "03"
files_modified:
  - apps/desktop/src/ui/dashboard/DashboardBuilderModel.ts
  - apps/desktop/src/ui/dashboard/DashboardLivePreviewModel.ts
  - tests/dashboard/dashboard-builder-live-preview-integration.spec.ts
autonomous: true
requirements:
  - DASH-03
  - DASH-05
must_haves:
  truths:
    - User can reorder tiles from builder controls, save layout, and see the new order persist in subsequent model reads.
    - User sees live preview order/content updates immediately while editing and reordering tiles in the PC builder.
    - Saved layout order remains contiguous and stable across builder and preview surfaces.
  artifacts:
    - path: apps/desktop/src/ui/dashboard/DashboardBuilderModel.ts
      provides: Builder handlers for reorder + save behavior and persisted ordering state.
    - path: apps/desktop/src/ui/dashboard/DashboardLivePreviewModel.ts
      provides: Preview integration that reflects reorder/edit events from builder-driven runtime mutations.
    - path: tests/dashboard/dashboard-builder-live-preview-integration.spec.ts
      provides: End-to-end assertions for reorder persistence and real-time preview synchronization.
  key_links:
    - from: apps/desktop/src/ui/dashboard/DashboardBuilderModel.ts
      to: apps/desktop/src/runtime/connectivity/desktop-connectivity-runtime.ts
      via: Builder reorder/save commands call runtime reorder APIs and read persisted snapshot.
      pattern: reorderDashboardTiles|getDashboardLayout|save|dirty
    - from: apps/desktop/src/ui/dashboard/DashboardBuilderModel.ts
      to: apps/desktop/src/ui/dashboard/DashboardLivePreviewModel.ts
      via: Builder mutations trigger runtime events consumed by preview subscriptions.
      pattern: subscribe|onSnapshot|tiles|order
---

<objective>
Complete reorder-and-save workflows and prove builder actions drive real-time preview updates from one runtime layout source.

Purpose: close the final user-visible Phase 3 behavior by demonstrating persisted ordering and trustworthy live preview while editing.
Output: builder reorder/save model updates, preview integration refinements, and end-to-end dashboard builder/preview integration tests.
</objective>

<execution_context>
@C:/Users/user/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/user/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-dashboard-builder-and-live-preview/03-RESEARCH.md
@.planning/phases/03-dashboard-builder-and-live-preview/03-02-SUMMARY.md
@.planning/phases/03-dashboard-builder-and-live-preview/03-03-SUMMARY.md
@apps/desktop/src/ui/dashboard/DashboardBuilderModel.ts
@apps/desktop/src/ui/dashboard/DashboardLivePreviewModel.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add builder reorder and save workflow semantics with persisted runtime snapshots</name>
  <files>apps/desktop/src/ui/dashboard/DashboardBuilderModel.ts</files>
  <action>Extend dashboard builder handlers with explicit `moveTile(fromIndex,toIndex)` and `saveLayout` semantics that route through runtime reorder APIs, maintain deterministic `isDirty` state, and reload persisted snapshots after save. Ensure reorder operations are tile-ID aware (not index identity) and preserve canonical contiguous order values from runtime responses.</action>
  <verify>
    <automated>npm run test -- tests/dashboard/dashboard-builder-live-preview-integration.spec.ts --runInBand -t "reorder and save"</automated>
    <manual>Confirm reloading the builder model after save returns the same tile order that user saved.</manual>
    <sampling_rate>Run after task commit and before Task 2.</sampling_rate>
  </verify>
  <done>Builder model supports reorder/save workflows with persisted ordering and deterministic dirty-state transitions.</done>
</task>

<task type="auto">
  <name>Task 2: Tighten live preview integration for builder-driven reorder and edit events</name>
  <files>apps/desktop/src/ui/dashboard/DashboardLivePreviewModel.ts</files>
  <action>Update live preview integration to ensure builder-driven create/edit/reorder/delete mutations all propagate through runtime subscriptions to preview state without manual refresh. Keep rendering order consistent with runtime snapshot order and preserve unsubscribe behavior from Plan 03.</action>
  <verify>
    <automated>npm run test -- tests/dashboard/dashboard-builder-live-preview-integration.spec.ts --runInBand -t "live preview updates"</automated>
    <manual>Confirm preview reflects label/icon edits and reorder changes immediately after builder operations complete.</manual>
    <sampling_rate>Run after task commit and before Task 3.</sampling_rate>
  </verify>
  <done>Live preview mirrors builder edits and reorder actions in real time from runtime events.</done>
</task>

<task type="auto">
  <name>Task 3: Add end-to-end integration tests for reorder persistence and real-time builder preview sync</name>
  <files>tests/dashboard/dashboard-builder-live-preview-integration.spec.ts</files>
  <action>Create integration-style tests that wire runtime + desktop builder + live preview models and verify: reorder persistence across save/reload, delete/create/edit mutation correctness, and immediate preview updates while editing. Include assertions that order indices remain contiguous and identical between builder and preview snapshots after each mutation sequence.</action>
  <verify>
    <automated>npm run test -- tests/dashboard/dashboard-builder-live-preview-integration.spec.ts --runInBand</automated>
    <manual>Confirm DASH-03 and DASH-05 truths are directly covered with deterministic assertions.</manual>
    <sampling_rate>Run after task commit and before plan completion.</sampling_rate>
  </verify>
  <done>Automated integration tests prove reorder persistence and live preview synchronization from builder-driven runtime edits.</done>
</task>

</tasks>

<verification>
- Builder reorder/save workflow persists tile order and keeps contiguous runtime ordering.
- Live preview updates in real time for create/edit/reorder/delete changes initiated from builder handlers.
- Integration suite verifies builder and preview stay synchronized after full mutation sequences.
</verification>

<success_criteria>
- DASH-03 and DASH-05 are satisfied by runtime-backed builder and live-preview integration behavior.
- `npm run test -- tests/dashboard/dashboard-builder-live-preview-integration.spec.ts --runInBand` passes.
</success_criteria>

<output>
After completion, create `.planning/phases/03-dashboard-builder-and-live-preview/03-04-SUMMARY.md`
</output>
