---
phase: 03-dashboard-builder-and-live-preview
plan: "02"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - apps/desktop/src/ui/dashboard/DashboardBuilderModel.ts
  - apps/desktop/src/ui/control-panel/DesktopControlPanelModel.ts
  - tests/ui/dashboard-builder-model.spec.ts
autonomous: true
requirements:
  - DASH-01
  - DASH-02
  - DASH-04
must_haves:
  truths:
    - User can create dashboard tiles from the desktop control panel with label and icon inputs.
    - User can edit tile label, icon, and mapped action values from desktop builder controls.
    - User can delete a tile from desktop builder controls and immediately see it removed from the builder model.
  artifacts:
    - path: apps/desktop/src/ui/dashboard/DashboardBuilderModel.ts
      provides: Desktop builder runtime model and mutation handlers for tile create/edit/delete.
    - path: apps/desktop/src/ui/control-panel/DesktopControlPanelModel.ts
      provides: Control panel model composition that includes dashboard builder state.
    - path: tests/ui/dashboard-builder-model.spec.ts
      provides: Automated UI-model verification of create/edit/delete flows.
  key_links:
    - from: apps/desktop/src/ui/dashboard/DashboardBuilderModel.ts
      to: apps/desktop/src/runtime/connectivity/desktop-connectivity-runtime.ts
      via: Builder handlers call runtime dashboard APIs for all mutations.
      pattern: createDashboardTile|updateDashboardTile|deleteDashboardTile|getDashboardLayout
    - from: apps/desktop/src/ui/control-panel/DesktopControlPanelModel.ts
      to: apps/desktop/src/ui/dashboard/DashboardBuilderModel.ts
      via: Control panel runtime model exposes builder model alongside existing panels.
      pattern: dashboardBuilder|create.*RuntimeModel|handlers
---

<objective>
Add desktop dashboard-builder runtime models that drive tile create/edit/delete through runtime APIs instead of UI-local state.

Purpose: deliver user-facing tile management workflows in the PC control panel while preserving deterministic runtime ownership.
Output: dashboard builder model/handlers, control panel integration, and focused UI-model tests for CRUD behavior.
</objective>

<execution_context>
@C:/Users/user/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/user/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-dashboard-builder-and-live-preview/03-RESEARCH.md
@.planning/phases/03-dashboard-builder-and-live-preview/03-01-SUMMARY.md
@apps/desktop/src/ui/control-panel/DesktopControlPanelModel.ts
@apps/desktop/src/ui/trusted-devices/TrustedDevicesPanel.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build dashboard builder runtime model and handlers for create/edit/delete operations</name>
  <files>apps/desktop/src/ui/dashboard/DashboardBuilderModel.ts</files>
  <action>Create a dashboard-builder model module that exposes current tiles, form-ready editor state, and mutation handlers (`createTile`, `updateTile`, `deleteTile`) backed by `DesktopConnectivityRuntime` dashboard APIs. Keep tile identity stable by tile ID, include explicit error labels for validation/not-found outcomes, and ensure mapped action edits reuse shared action contract fields. Do not implement reorder behavior in this plan.</action>
  <verify>
    <automated>npm run test -- tests/ui/dashboard-builder-model.spec.ts --runInBand -t "create update delete"</automated>
    <manual>Confirm builder handlers return deterministic user-facing status labels for success and validation failures.</manual>
    <sampling_rate>Run after task commit and before Task 2.</sampling_rate>
  </verify>
  <done>Desktop builder model supports deterministic create/edit/delete flows through runtime APIs.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate dashboard builder model into desktop control-panel runtime model</name>
  <files>apps/desktop/src/ui/control-panel/DesktopControlPanelModel.ts</files>
  <action>Extend `DesktopControlPanelRuntimeModel` and runtime handlers to expose dashboard-builder data and operations alongside existing connection/trusted-device/action-history sections. Preserve existing panel behavior and keep builder access asynchronous where runtime calls are required. Avoid introducing direct runtime mutations from UI code paths outside the builder handlers.</action>
  <verify>
    <automated>npm run test -- tests/ui/desktop-control-panel-model.spec.ts tests/ui/dashboard-builder-model.spec.ts --runInBand -t "dashboard builder"</automated>
    <manual>Confirm the control panel model includes a dashboard builder section without regressing trusted devices/action history sections.</manual>
    <sampling_rate>Run after task commit and before Task 3.</sampling_rate>
  </verify>
  <done>Control panel runtime model includes dashboard-builder state and operations while preserving existing sections.</done>
</task>

<task type="auto">
  <name>Task 3: Add desktop builder UI-model tests for tile create/edit/delete behavior</name>
  <files>tests/ui/dashboard-builder-model.spec.ts</files>
  <action>Add tests that instantiate runtime + builder model and verify: tile creation with label/icon, tile update for label/icon/action mapping, deletion by tile ID, and deterministic handling for invalid payload or missing tile. Assert model snapshots come from runtime responses rather than mutating local arrays.</action>
  <verify>
    <automated>npm run test -- tests/ui/dashboard-builder-model.spec.ts --runInBand</automated>
    <manual>Confirm all DASH-01/DASH-02/DASH-04 desktop workflows have explicit assertions in this suite.</manual>
    <sampling_rate>Run after task commit and before plan completion.</sampling_rate>
  </verify>
  <done>Automated tests prove desktop tile create/edit/delete workflows are runtime-backed and deterministic.</done>
</task>

</tasks>

<verification>
- Desktop builder can create, edit, and delete tiles without bypassing runtime validation.
- Control panel runtime model exposes dashboard builder state with existing panel sections intact.
- Builder tests cover success and deterministic failure outcomes for user-facing CRUD interactions.
</verification>

<success_criteria>
- DASH-01, DASH-02, and DASH-04 desktop-builder flows are implemented and test-covered.
- `npm run test -- tests/ui/dashboard-builder-model.spec.ts --runInBand` passes.
</success_criteria>

<output>
After completion, create `.planning/phases/03-dashboard-builder-and-live-preview/03-02-SUMMARY.md`
</output>
