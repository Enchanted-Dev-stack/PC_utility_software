---
phase: 03-dashboard-builder-and-live-preview
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - shared/src/contracts/dashboard/dashboard-tile.ts
  - apps/desktop/src/runtime/dashboard/dashboard-layout-store.ts
  - apps/desktop/src/runtime/dashboard/dashboard-layout-events.ts
  - apps/desktop/src/runtime/dashboard/dashboard-layout-service.ts
  - apps/desktop/src/runtime/connectivity/desktop-connectivity-runtime.ts
  - tests/dashboard/dashboard-layout-service.spec.ts
autonomous: true
requirements:
  - DASH-01
  - DASH-02
  - DASH-03
  - DASH-04
must_haves:
  truths:
    - Desktop runtime can create a tile with label and icon using deterministic IDs and validated action mapping.
    - Desktop runtime can edit and delete tiles by stable tile ID and returns predictable not-found outcomes.
    - Desktop runtime can reorder tiles and return a contiguous saved order (`0..n-1`) after every move.
  artifacts:
    - path: shared/src/contracts/dashboard/dashboard-tile.ts
      provides: Typed tile contract and validation helpers aligned with curated action types.
    - path: apps/desktop/src/runtime/dashboard/dashboard-layout-store.ts
      provides: Runtime-owned immutable dashboard snapshot state with CRUD + reorder operations.
    - path: apps/desktop/src/runtime/dashboard/dashboard-layout-service.ts
      provides: Validated service boundary returning deterministic operation results.
    - path: apps/desktop/src/runtime/connectivity/desktop-connectivity-runtime.ts
      provides: Runtime composition exposing dashboard layout APIs to desktop/mobile clients.
    - path: tests/dashboard/dashboard-layout-service.spec.ts
      provides: Automated verification for create/edit/reorder/delete and validation outcomes.
  key_links:
    - from: apps/desktop/src/runtime/dashboard/dashboard-layout-service.ts
      to: shared/src/contracts/dashboard/dashboard-tile.ts
      via: Service validates create/update payloads against shared dashboard contract.
      pattern: validate|DASHBOARD|ACTION_TYPES|media_control|open_website|open_app
    - from: apps/desktop/src/runtime/connectivity/desktop-connectivity-runtime.ts
      to: apps/desktop/src/runtime/dashboard/dashboard-layout-service.ts
      via: Runtime composes dashboard service and exposes typed mutation/query methods.
      pattern: dashboard|createTile|updateTile|reorderTiles|deleteTile
---

<objective>
Establish a runtime-owned dashboard layout foundation with typed tile contracts, validated CRUD operations, and deterministic ordering.

Purpose: make dashboard editing a first-class runtime capability so desktop builder and mobile preview read the same authoritative snapshot.
Output: shared dashboard contracts, runtime store/events/service modules, runtime wiring methods, and integration-style runtime tests.
</objective>

<execution_context>
@C:/Users/user/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/user/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-dashboard-builder-and-live-preview/03-RESEARCH.md
@shared/src/contracts/actions/action-command.ts
@apps/desktop/src/runtime/actions/action-feedback-events.ts
@apps/desktop/src/runtime/connectivity/desktop-connectivity-runtime.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add shared dashboard tile contract and centralized payload validation rules</name>
  <files>shared/src/contracts/dashboard/dashboard-tile.ts</files>
  <action>Create a new shared dashboard contract module that defines tile identity, icon token shape, tile ordering fields, and mapped action payload unions that reuse existing curated action types (`open_app`, `open_website`, `media_control`). Include deterministic validation helpers for create/update inputs so invalid labels/icons/action payloads are rejected before runtime mutation. Do not introduce new action categories or Phase 4 scope.</action>
  <verify>
    <automated>npm run test -- tests/dashboard/dashboard-layout-service.spec.ts --runInBand -t "contract validation"</automated>
    <manual>Confirm invalid action payloads are rejected with explicit validation outcomes, not generic exceptions.</manual>
    <sampling_rate>Run after task commit and before Task 2.</sampling_rate>
  </verify>
  <done>Shared dashboard contracts and validators exist and enforce Phase 3 allowed tile/action shapes.</done>
</task>

<task type="auto">
  <name>Task 2: Implement runtime dashboard layout store, events, and service operations</name>
  <files>apps/desktop/src/runtime/dashboard/dashboard-layout-store.ts, apps/desktop/src/runtime/dashboard/dashboard-layout-events.ts, apps/desktop/src/runtime/dashboard/dashboard-layout-service.ts</files>
  <action>Implement an immutable runtime `DashboardLayoutStore` with create, update, reorder, and delete operations keyed by tile ID. Add `DashboardLayoutEvents` with typed `subscribe(listener) => unsubscribe` semantics and emit cloned snapshots after each successful mutation. Build a service layer that orchestrates validation, deterministic `idFactory`/`now` injection for tests, not-found handling, and canonical reindexing after reorder (`0..n-1`). Avoid dual state ownership and index-based tile identity.</action>
  <verify>
    <automated>npm run test -- tests/dashboard/dashboard-layout-service.spec.ts --runInBand -t "store service operations"</automated>
    <manual>Confirm reorder outputs contiguous order values and deletes remove only the targeted tile by ID.</manual>
    <sampling_rate>Run after task commit and before Task 3.</sampling_rate>
  </verify>
  <done>Runtime dashboard service emits deterministic immutable snapshots for create/edit/reorder/delete operations.</done>
</task>

<task type="auto">
  <name>Task 3: Wire dashboard runtime APIs into desktop connectivity runtime and verify behavior</name>
  <files>apps/desktop/src/runtime/connectivity/desktop-connectivity-runtime.ts, tests/dashboard/dashboard-layout-service.spec.ts</files>
  <action>Compose the dashboard layout service/events inside `DesktopConnectivityRuntime` and expose typed query/mutation/subscription methods for clients (`getDashboardLayout`, `createDashboardTile`, `updateDashboardTile`, `reorderDashboardTiles`, `deleteDashboardTile`, `subscribeDashboardLayout`). Extend dashboard tests to cover runtime-level wiring so callers observe the same snapshot stream exposed by the service.</action>
  <verify>
    <automated>npm run test -- tests/dashboard/dashboard-layout-service.spec.ts --runInBand</automated>
    <manual>Confirm runtime callers can mutate and immediately read updated snapshots without touching UI-local state.</manual>
    <sampling_rate>Run after task commit and before plan completion.</sampling_rate>
  </verify>
  <done>Desktop runtime exposes dashboard CRUD/reorder/delete APIs and emits synchronized layout snapshots to subscribers.</done>
</task>

</tasks>

<verification>
- Runtime-owned dashboard APIs support create, edit, reorder, and delete with deterministic, validated outcomes.
- Tile ordering is canonicalized after each move and persists in returned snapshots.
- All dashboard mutations emit typed snapshot events through subscribe/unsubscribe lifecycle.
</verification>

<success_criteria>
- DASH-01, DASH-02, DASH-03, and DASH-04 runtime foundations are implemented in shared/runtime layers.
- `npm run test -- tests/dashboard/dashboard-layout-service.spec.ts --runInBand` passes.
</success_criteria>

<output>
After completion, create `.planning/phases/03-dashboard-builder-and-live-preview/03-01-SUMMARY.md`
</output>
