---
phase: 03-dashboard-builder-and-live-preview
plan: "03"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - apps/desktop/src/ui/dashboard/DashboardLivePreviewModel.ts
  - apps/mobile/src/ui/dashboard/MobileDashboardModel.ts
  - apps/mobile/src/runtime/connectivity/mobile-connectivity-client.ts
  - tests/dashboard/dashboard-live-preview-model.spec.ts
autonomous: true
requirements:
  - DASH-05
must_haves:
  truths:
    - Mobile preview model receives dashboard snapshot updates from runtime subscriptions in real time.
    - Desktop live-preview model and mobile model render from the same runtime snapshot shape.
    - Subscriptions can be torn down cleanly so duplicate listeners do not accumulate.
  artifacts:
    - path: apps/desktop/src/ui/dashboard/DashboardLivePreviewModel.ts
      provides: Desktop-side live preview model fed by runtime dashboard snapshot events.
    - path: apps/mobile/src/ui/dashboard/MobileDashboardModel.ts
      provides: Mobile dashboard model built from runtime layout snapshots.
    - path: apps/mobile/src/runtime/connectivity/mobile-connectivity-client.ts
      provides: Mobile client APIs to read and subscribe to runtime dashboard layouts.
    - path: tests/dashboard/dashboard-live-preview-model.spec.ts
      provides: Automated verification of real-time snapshot propagation and unsubscribe behavior.
  key_links:
    - from: apps/mobile/src/runtime/connectivity/mobile-connectivity-client.ts
      to: apps/desktop/src/runtime/connectivity/desktop-connectivity-runtime.ts
      via: Mobile client bridges runtime layout read/subscribe APIs.
      pattern: getDashboardLayout|subscribeDashboardLayout
    - from: apps/desktop/src/ui/dashboard/DashboardLivePreviewModel.ts
      to: apps/mobile/src/ui/dashboard/MobileDashboardModel.ts
      via: Both models consume the same normalized dashboard snapshot contract.
      pattern: tiles|layoutVersion|subscribe|unsubscribe
---

<objective>
Implement live-preview read and subscription models so desktop and mobile dashboard views stay synchronized to runtime layout changes.

Purpose: deliver the technical backbone for real-time preview (DASH-05) without duplicating dashboard state in UI layers.
Output: desktop live-preview model, mobile dashboard model/client APIs, and live-preview subscription tests.
</objective>

<execution_context>
@C:/Users/user/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/user/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-dashboard-builder-and-live-preview/03-RESEARCH.md
@.planning/phases/03-dashboard-builder-and-live-preview/03-01-SUMMARY.md
@apps/mobile/src/runtime/connectivity/mobile-connectivity-client.ts
@apps/desktop/src/runtime/actions/action-feedback-events.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create desktop live-preview model from runtime dashboard snapshots</name>
  <files>apps/desktop/src/ui/dashboard/DashboardLivePreviewModel.ts</files>
  <action>Create a desktop live-preview model that reads the current dashboard layout snapshot and subscribes to layout updates from runtime handlers. Include lightweight tile rendering metadata (label, icon, order, action summary) and expose a clean unsubscribe path. Keep this module read-oriented; mutation actions remain in builder runtime handlers.</action>
  <verify>
    <automated>npm run test -- tests/dashboard/dashboard-live-preview-model.spec.ts --runInBand -t "desktop live preview"</automated>
    <manual>Confirm live preview model updates immediately when runtime snapshot changes without polling loops.</manual>
    <sampling_rate>Run after task commit and before Task 2.</sampling_rate>
  </verify>
  <done>Desktop live preview model reflects runtime snapshot changes and supports explicit subscription teardown.</done>
</task>

<task type="auto">
  <name>Task 2: Add mobile dashboard model and connectivity client APIs for layout subscription</name>
  <files>apps/mobile/src/ui/dashboard/MobileDashboardModel.ts, apps/mobile/src/runtime/connectivity/mobile-connectivity-client.ts</files>
  <action>Add mobile dashboard model builders that map runtime tile snapshots into mobile-ready tile view models. Extend `MobileConnectivityClient` with `getDashboardLayout` and `subscribeDashboardLayout` APIs that proxy runtime methods and preserve unsubscribe semantics. Keep existing pairing/connectivity/action APIs unchanged.</action>
  <verify>
    <automated>npm run test -- tests/dashboard/dashboard-live-preview-model.spec.ts --runInBand -t "mobile client subscription"</automated>
    <manual>Confirm mobile model receives exactly one update per runtime emit and stops receiving updates after unsubscribe.</manual>
    <sampling_rate>Run after task commit and before Task 3.</sampling_rate>
  </verify>
  <done>Mobile runtime client and model can read and subscribe to dashboard layout snapshots with leak-safe teardown.</done>
</task>

<task type="auto">
  <name>Task 3: Add live-preview model tests for real-time propagation and unsubscribe safety</name>
  <files>tests/dashboard/dashboard-live-preview-model.spec.ts</files>
  <action>Add tests proving dashboard snapshot updates propagate to desktop and mobile preview models in real time, that both surfaces render consistent tile ordering/content, and that calling unsubscribe prevents further updates (no duplicate callback accumulation across re-subscribe cycles).</action>
  <verify>
    <automated>npm run test -- tests/dashboard/dashboard-live-preview-model.spec.ts --runInBand</automated>
    <manual>Confirm DASH-05 technical behavior is asserted with runtime-driven updates, not manual model mutation.</manual>
    <sampling_rate>Run after task commit and before plan completion.</sampling_rate>
  </verify>
  <done>Automated tests validate runtime-driven live preview synchronization and subscription lifecycle correctness.</done>
</task>

</tasks>

<verification>
- Desktop and mobile preview models consume the same runtime snapshot structure.
- Live preview updates are event-driven and immediate after runtime mutation events.
- Subscription teardown prevents duplicate event handling over repeated subscribe cycles.
</verification>

<success_criteria>
- DASH-05 preview synchronization foundation is implemented for desktop and mobile models.
- `npm run test -- tests/dashboard/dashboard-live-preview-model.spec.ts --runInBand` passes.
</success_criteria>

<output>
After completion, create `.planning/phases/03-dashboard-builder-and-live-preview/03-03-SUMMARY.md`
</output>
